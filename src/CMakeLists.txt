
# We will have one library for each subdirectory in this folder - one
# subdirectory for one sub-namespace of wq:: namespace.
# Hint: variable WQ_OUTPUT_LIBS_PATH contains the path of directory were libs should be placed.

