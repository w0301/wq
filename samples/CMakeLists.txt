
# Here are listed all samples directories.

# First sample is very simple sample, it's CMakeLists.txt file is in subdir simple.
add_subdirectory(simple/)

# Sample which generates C/C++ arrays from unicode mapping files.
add_subdirectory(tables_gen/)

